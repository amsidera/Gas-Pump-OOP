package output.actions;

import data.Data;

/*
 * GasPump1 StoreCash action responsible for storing
 * cash value into the data store.
 */

public class StoreCash1 implements StoreCash{
	
	/*
	 * Since GasPump1 doesn't work with cash
	 * no implementation is needed
	 */
	@Override
	public void act(Data d) {
	}
}